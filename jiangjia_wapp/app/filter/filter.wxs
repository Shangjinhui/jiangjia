// var removeStr = function(item) {
//   //return item.replace(/[^0-9]/ig,"")
//   let arr = item.split(''),str = '',i;
//   for(i=0;i<arr.length;i++){
//     if(Number(arr[i])) str+=arr[i]
//   }
//   return str;
// }
// module.exports.removeStr = removeStr;
function indexOf(str, ft) {
  return str.indexOf(ft);
}
module.exports = {
  indexOf: indexOf
}